# NUNCA commitar este arquivo com valores reais!
# Copie para secrets.yaml e preencha os valores (secrets.yaml está no .gitignore)
apiVersion: v1
kind: Secret
metadata:
  name: agent-secrets
  namespace: agent-platform
type: Opaque
stringData:
  anthropic-api-key: "sk-ant-COLOQUE-SUA-CHAVE-AQUI"
